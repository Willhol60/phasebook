<body>
  <div class="row">
    <div class="col-sm" id="left-third">
      <h1>Newsfeed</h1>
    </div>
    <div class="col-sm" id="middle-third">
      <% @readings.each do |reading| %>
        <% if reading.read_status == 'Finished' %>

          <%# CARD: %>
          <div id="container card-product">
            <%# <img src= <%= reading.book.poster_url %>
            <div id="card-product-infos">

              <div id="user-info">
                <%= image_tag (reading.user.profile_image), class: "avatar" %>
                <div id="user-full-name">
                  <%= reading.user.first_name %> <%= reading.user.last_name %>
                </div>
                <div id="post-time">
                  Just now
                </div>
              </div>

              <div id="reading-info">
                <% if reading.end_date && reading.start_date %>
                  <p>days to read:<%= (reading.end_date - reading.start_date).to_i %></p>
                <% end %>
                <%# <%= reading.start_date %>
                <%# <%= reading.end_date %>
                <%= reading.book.poster_url %>
                <p><%= reading.book.title %></p>
                <p><%= reading.book.author %></p>
                <p><%= reading.book.pages %> pages</p>
                <p>User rating: <%= reading.user_rating %></p>
              </div>

            </div>
            <div>
              <div class="cheers">
                <p><%= reading.cheers %> cheers</p>
                <button></button>
              </div>
            </div>
          </div>

          <div class="container comment-window">
            <% reading.comments.each do |comment| %>
              <div class="comment-row">
                <%= image_tag (comment.user.profile_image), class: "avatar" %>
                <div class="comment">
                  <div class="comment-first-line">
                    <p><strong><%= comment.user.first_name %> <%= comment.user.last_name %></strong></p>
                    <p><%= (comment.created_at).strftime('%A, %d %b %Y %l:%M %p') %></p>
                    <%# <p><%= comment.updated_at %>
                  </div>
                  <p><%= comment.content %></p>
                </div>
              </div>
            <% end %>
            <%= simple_form_for [reading.book, reading, @comment] do |f| %>
              <%= f.input :content, placeholder: 'Comment here...', label:false %>
              <%= f.submit %>
            <% end %>
            <%# simple form text input to add a comment - in 'comment new' view page %>
          </div>
          <%# END OF CARD %>

        <% end %>
      <% end %>
    </div>
    <div class="col-sm" id="right-third">

    </div>
  </div>
</body>
